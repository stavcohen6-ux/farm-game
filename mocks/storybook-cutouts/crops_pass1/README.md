# Wave 3 crops — pass 1 (rejected)

Rejected Soft B plantable crop mocks.

**Why rejected:** Style references included live `assets/icons/*` crop art,
so outputs bled toward the old painterly lock instead of Soft Storybook B
(fox-shrine-b + layered shrines).

Kept forever — do not delete.
